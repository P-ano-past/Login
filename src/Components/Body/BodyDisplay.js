import React, { Component } from "react";
import axios from "axios";

// make an api request to get the hash from mongo.
// implement a switch that shows the password hash and a button that translates the password hash .
export default class BodyDisplay extends Component {
  render() {
    return <div></div>;
  }
}
